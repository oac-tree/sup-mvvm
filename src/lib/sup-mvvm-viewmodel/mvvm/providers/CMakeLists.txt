# Components for Qt models (selection models, delegates)

target_sources(${library_name} PRIVATE
  default_cell_decorator.cpp
  default_cell_decorator.h
  viewmodel_delegate.cpp
  viewmodel_delegate.h
  )
