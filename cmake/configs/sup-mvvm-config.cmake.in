#  SUP_MVVM_INCLUDE_DIRS - include directories for qt-mvvm
#  SUP_MVVM_LIBRARIES    - libraries to link against

get_filename_component(SUP_MVVM_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)

include(CMakeFindDependencyMacro)

find_dependency(QT NAMES Qt6 Qt5 REQUIRED COMPONENTS Widgets Core Gui PrintSupport)
find_dependency(Qt${QT_VERSION_MAJOR} REQUIRED COMPONENTS Widgets Core Gui PrintSupport)
#find_dependency(Qt5 5.12 COMPONENTS Widgets REQUIRED)

find_dependency(Threads)
find_dependency(LibXml2)

if(TARGET sup-mvvm::model OR TARGET sup-mvvm::viewmodel OR TARGET sup-mvvm::view)
else()
  message(STATUS "sup-mvvm-config.cmake including ${SUP_MVVM_CMAKE_DIR}/sup-mvvm-targets.cmake")
 include("${SUP_MVVM_CMAKE_DIR}/sup-mvvm-targets.cmake")
endif()

set(SUP_MVVM_LIBRARIES sup-mvvm::model sup-mvvm::viewmodel sup-mvvm:view)

message(STATUS "sup-mvvm-config.cmake in ${CMAKE_CURRENT_LIST_FILE}")
message(STATUS "SUP_MVVM_LIBRARIES ${SUP_MVVM_LIBRARIES}")


